# todolist-webpage
it's a simple 'todolist' web application, with the use of Jquery(a functional packaged libray from javascript with better syntax to use) and Bootstrap(styling package for css html) to handle to front-end of basic styling problem. Using the Koa web framework for Node.js, with it's clean and easy way to handle the back-end manipulation.
